services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: stickerswap
      POSTGRES_USER: stickerswap
      POSTGRES_PASSWORD: stickerswap
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
